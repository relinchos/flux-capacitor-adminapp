describe('multiModelProcess', function() {

  beforeEach(module('adminApp'));

  it('should ...', inject(function(multiModelProcess) {

	//expect(multiModelProcess.doSomething()).toEqual('something');

  }));

});